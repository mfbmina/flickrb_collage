require "spec_helper"

describe FlickrbCollage do
  it "has a version number" do
    expect(FlickrbCollage::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
